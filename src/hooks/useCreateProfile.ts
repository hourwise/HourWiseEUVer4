/**
 * DEFUNCT HOOK
 * Profile creation is now handled centrally in src/providers/AuthProvider.tsx
 * to support rich onboarding data (Fleet Invites, Payroll Numbers, etc.).
 */
export const useCreateProfile = () => {
  // Logic disabled to prevent duplicate profile conflicts.
  return null;
};
